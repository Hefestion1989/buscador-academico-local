# Dependencias directas fijadas para que una instalación nueva use las mismas
# versiones probadas. Dependabot puede proponer actualizaciones controladas.
streamlit==1.60.0
# Chroma se usa solo como PersistentClient embebido. No iniciar `chroma run`
# ni exponer su API HTTP: 1.5.9 tiene GHSA-f4j7-r4q5-qw2c y todavia no hay
# una version corregida publicada. Revisar este pin cuando aparezca el parche.
chromadb==1.5.9
sentence-transformers==5.6.1
pypdf==6.14.2
PyMuPDF==1.28.0
python-docx==1.2.0
